{
  "name": "duckdb-s3-access-levels",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "ts-node src/update-access-levels.ts",
    "start": "ts-node src/index.ts",
    "build:lambda": "tsc -p tsconfig.lambda.json",
    "build:duckdb-layer": "rm -rf layers/duckdb/nodejs/node_modules layers/duckdb/nodejs/package-lock.json && npm_config_target_platform=linux npm_config_target_arch=x64 npm_config_platform=linux npm_config_arch=x64 npm install --prefix layers/duckdb/nodejs --omit=dev",
    "sls:package": "npm run build:duckdb-layer && npm run build:lambda && serverless package",
    "sls:deploy": "npm run build:duckdb-layer && npm run build:lambda && serverless deploy",
    "sls:remove": "serverless remove"
  },
  "dependencies": {
    "datalayer": "file:./datalayer"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "serverless": "^3.40.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
